<% if obj.errors.any? %>
  <p><b>The following errors prevented the <%= obj.class.name.downcase %> from being saved</b></p>
  <ul>
    <% obj.errors.full_messages.each do |msg| %>
      <li><mark><%= msg %></mark></li>
    <% end %>
  </ul>
  <hr>
<% end %>